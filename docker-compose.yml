services:
  katulong:
    build: .
    env_file:
      - path: .env.local
        required: false
    ports:
      - "${PORT:-3001}:3001"
    volumes:
      - katulong-data:/data
    environment:
      - KATULONG_DATA_DIR=/data
    restart: unless-stopped

volumes:
  katulong-data:
